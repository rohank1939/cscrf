/** @type {import('next').NextConfig} */
const nextConfig = {
  // Optional: Add any specific Next.js configurations here
  // For example, if you need to handle images or internationalization.
};

module.exports = nextConfig;
